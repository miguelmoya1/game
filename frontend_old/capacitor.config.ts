import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'game.miguelmo.dev',
  appName: 'frontend',
  webDir: 'dist/frontend/browser',
};

export default config;
